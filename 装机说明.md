# Scan3MF · 装机说明（iPhone 13 Pro / iOS 16.3）

**你的设备已确认：iPhone 13 Pro，iOS 16.3 → 巨魔（TrollStore）可安装 ✓**

---

## 你现在要做的三件事

### ① 上传工程到 GitHub

1. 打开 github.com → 登录 → 右上角 `+` → **New repository**
2. 名称填 `scan3mf`，选 **Public**，点 **Create repository**
3. 点页面上的 **uploading an existing file**
4. 打开 `C:\Users\xiaoc\Desktop\LiDARScan3MF\`，**全选里面所有内容**拖进网页
   - **必须包含 `.github` 文件夹**（这是编译机器人，少了它就没有编译按钮）
   - Windows 资源管理器默认可能不显示以点开头的文件夹：查看 → 显示 → 勾选「隐藏的项目」
5. 底部点 **Commit changes**

### ② 让 GitHub 免费帮你编译

6. 仓库顶部点 **Actions**
7. 左栏点 **Build TIPA** → 右侧 **Run workflow** → 再点绿色 **Run workflow**
8. 约 3–5 分钟。刷新页面，出现带绿勾的运行记录 → 点进去
9. 页面下方 **Artifacts** → 下载 **LiDARScan3MF-tipa**
10. 解压得到 **`LiDARScan3MF.tipa`** ← 这就是安装包

### ③ 装机

11. 把 `.tipa` 传进手机（微信文件传输助手 / 网盘 / 数据线都行）
12. 在手机上用**巨魔**打开这个文件 → Install

装完桌面出现 **Scan3MF**，首次打开会请求相机权限 —— 点允许。然后点「开始新扫描」就能扫了。

---

## 万一 `Run workflow` 那步报错

把报错截图发我。常见两种：

| 现象 | 原因 | 处理 |
|---|---|---|
| Actions 页面是空的，没有 Build TIPA | `.github` 文件夹没传上去 | 回到仓库确认根目录能看到 `.github`，没有就重新上传 |
| 编译报红 X | 代码在真实编译器上的兼容问题 | 截图发我，我改完你再重跑 |

**注意**：我在 Windows 上只能保证语法正确，没法真正编译验证。第一次云编译有点小报错是正常的，把日志发我，我逐个修——你有无限次重跑的机会，不花钱。

---

## 装完之后

- **扫描**：点首页「开始新扫描」，环绕物体缓慢移动，覆盖率到 90% 以上再点「完成」
- **编辑**：降噪 / 补洞 / 简化 / 平滑 都能用，「裁剪」是占位
- **导出**：默认 3MF（切片器首选），可选比例 1:2、1:1、2:1 和单位 mm/cm/m
- **文件位置**：App 沙盒 `Documents/models/` 和 `Documents/exports/`，导出时弹出系统分享面板，可以存到「文件」App 再传电脑

---

## 已知局限（MVP）

- 纹理是**最后一帧相机图像**逐顶点采色的近似做法，"深度融合"开关在界面上，但逐帧融合烘焙是下一版
- 覆盖率百分比是引导值（按已扫面积 / 1.2㎡ 估算），不是精确仪器读数
- 反光、透明、纯黑表面 LiDAR 会漏扫

## 备用路线（如果哪天需要）

- **Mac**：`xcodebuild -project LiDARScan3MF.xcodeproj -target LiDARScan3MF -configuration Release -sdk iphoneos -destination 'generic/platform=iOS' CODE_SIGNING_ALLOWED=NO build`
- **Sideloadly**（Windows 可用，7 天重签）：需要**已签名**构建，与上面无签名流程不同，届时找我改流水线
